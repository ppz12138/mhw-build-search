# MHWilds 配装搜索器 - 项目记忆

## 技能体系（核心规则）

### 系列技能
- **来源**: 同系列技能名的防具2件装备时效果Ⅰ发动，4件时效果Ⅱ发动
- **等级**: 固定2级（Lv1=2件, Lv2=4件）
- **武器洗练**: 武器可出1个系列技能（但不影响防具件数计算）
- **数据位置**: `skills_data.json` → `系列技能`
- **SKILL_CAPS**: 统一为2
- **need_pieces 逻辑**: 系列技能 Lv1=2件/Lv2=4件；组合技能固定3件

### 组合技能
- **来源**: 同组合技能名的防具3件装备时发动
- **等级**: 固定1级（Lv1=3件）
- **武器洗练**: 武器可出1个组合技能
- **数据位置**: `skills_data.json` → `组合技能`
- **SKILL_CAPS**: 统一为1

### 武器技能（重要架构理解）
- **武器 = 独立组件**：武器由「系列技能 × 组合技能」两两连接组成，与防具、护石**平权**（都是装备来源）
- **固定武器技能 ≠ 技能需求**：固定=预筛选武器（只带该技能的武器进入匹配池），**不是**把该技能作为配装硬性需求
- **平权原则**：武器提供的系列/组合技能只作为"1件补充"（帮助凑已需求的件数），配装不强制达到该技能的等级
- **来源（洗练）**: 武器洗练可出1个系列技能（等级档位Lv2=效果I/Lv4=效果II）+ 1个组合技能（Lv3）
- **来源（珠子）**: 插武器孔的珠子（WEAPON_SK）

### 防具技能
- **来源**: 插防具孔的珠子 + 防具自带
- **等级**: 各技能不同，见 `skills_data.json` → `防具技能` 的 `max_lv`

### 插槽技能（LvN插槽/防具LvN插槽/武器LvN插槽）= 空插槽语义（重要）
- **语义**: `Lv1插槽 Lv3` = 配装完成后至少剩 3 个**空插槽**（可自由插珠），**不是**装备初始总孔位
- **判定位置**:
  - `fill_slots`（fast_search_v3.py ~738-767, 888-897）：从 fixed 需求解析 `armor_min`/`weapon_min`，从孔位池**物理预留**空槽，插珠后校验 `all_rem_a`/`all_rem_w` 空槽数 ≥ 需求 —— 这是唯一精确判定
  - `_check_deco_feasible`（~949-972）：用初始孔位计数做宽松中间检查（总孔位 ≥ 需求，不误杀）
  - 叶子 fixed 检查：`_is_slot_skill` → continue（由 fill_slots 兜底）
- **候选构建不生成插槽技能点**（2026-08-13 修正）：此前把初始孔位换算成 `Lv{n}插槽` 技能点进候选，导致 fs 里 `Lv1插槽=总孔位`（如 16），污染展示口径；现移除（防具/护石/武器三处），fs 插槽技能只来自叶子记录的空槽覆盖
- **结果记录覆盖式写入**：叶子按 `rem_a`/`rem_w` 空槽计数覆盖 `Lv{n}插槽`（防具+武器合计）与分侧值，任何读取方拿到空槽口径
- **前端**: 预留孔位面板 → `防具Lv1插槽` 等 fixed 需求 → fill_slots 预留；剩余孔位展示从 `rem_a`/`rem_w` 计算

## 架构

### 后端
- `gui_server.py`: HTTP服务器，端口8765，处理API请求
- `fast_search_v3.py`: DFS搜索引擎，位掩码+向量化优化
- `calc_v8_final.py`: 伤害计算模块
- 数据文件: `decos_cn.json`(珠子), `armors_cn.json`(防具), `my_charms.json`(护石), `charms_cn.json`(可 craft 护石), `skills_data.json`(技能数据)

### 前端
- `index.html`: 单页面应用，内嵌CSS+JS

### 关键API
- `/api/info`: 返回技能数据、分类、上限等
- `/api/custom_search`: 自定义搜索（支持 `auto_weapon_skill` 参数）
- `/api/query_extra`: 追加技能查询（流式 NDJSON）
- `/api/detail_calc`: 计算技能伤害详情（不搜索）

## 搜索逻辑（当前）
- `dfs_search()`: DFS回溯搜索，输入 fixed_skills(防具技能需求) + combo_skills(系列/组合技能需求)
- `dfs_search_auto_weapon()`: 武器技能自动匹配
- 系列技能通过防具件数验证（`verify_series`），不是通过珠子
- `NO_DECO_SK = SERIES_SK | GROUP_SK`: 这些技能不能用珠子插，只能靠防具件数

## 术语规范
- **套装技能**: 指系列技能（SERIES_SK）+ 组合技能（GROUP_SK），即 `NO_DECO_SK = SERIES_SK | GROUP_SK`
- **武器技能（珠子）**: 指插武器孔的珠子技能（WEAPON_SK）
- **防具技能（珠子）**: 指插防具孔的珠子技能 + 防具自带技能
- **武器洗练技能**: 指通过武器洗练获得的系列技能或组合技能（NO_DECO_SK），不是珠子

## 重要修复记录
- 系列技能上限从4改为2（硬编码错误，已用skills_data.json覆盖）
- 组合技能上限从3改为1（同上）
- detail_calc不执行搜索，直接基于已有技能计算
- 搜索超时移除（全量搜索）
- 武器技能预检查：允许防具提供武器技能，不误判无解
- 技能上限和分类从skills_data.json自动生成
- 2026-08-07 追加模式进度条修复：流式 NDJSON + Transfer-Encoding: chunked
- 2026-08-07 固定武器技能语义修复：前端 runSearch/runExtraQuery 不再把武器区技能加入 combo
- 2026-08-08 fill_slots 贪心插珠侵占预留槽位 bug 修复
- 2026-08-08 预检查预留槽重复计数 bug 修复
- 2026-08-13 系列/组合技能（NO_DECO_SK）武器件数双计 bug 修复：
  - 问题：DFS 放置武器时把武器 skills 计入 `_series_count`（_sp），而 `_weapon_series_cnt`/`_series_wprov`（武器必提供1件的假设）又计一次 → 黑蚀龙之力2 只需武器1件即可"通过"（1+1=2），实际只有1件
  - 修复：`_series_count`/`_series_have` 只累计防具件数（part_idx != 6）；`_cur_all_skills` 不再预置 `weapon_skills`（避免结果技能双计）；`remaining_series_max` 武器层在 `_series_wprov` 已给1时计0（保持上界剪枝紧致）
  - 效果：黑蚀龙之力2 必须真实 2 件（武器1 + 防具1，如黑蚀龙护腿β）；原方案（狱焰蛸护腿γ，仅武器1件）被正确判否，搜索找到真正满足的新方案（黑蚀龙护腿β），期望伤害 879.7
  - 性能：找第1方案 1.7s；该需求合法方案少，多方案枚举慢（90s 超时只出1个），属支配剪枝+约束紧致的固有代价，非回归（修复前 bench_baseline 无 combo 同样 30s 超时）

## 武器孔位匹配复合珠（2026-08-13 完整解决，勿再走错路）

**目标**：固定 15 点武器技能（格挡性能3/超会心3/会心击3/龙属强3/利刃3）+ 防具技能 + 系列/组合技能，1 秒内出方案。
**结果**：`_bench_full2.py` 1.0s 出 3 方案 ✅

### 参照项目（wiki-db）武器槽机制（源码实证）
1. **武器槽组合枚举** `Y.i()`：20 种组合（[3,3,3] → [0,0,0]），搜索不是固定 3 槽
2. **护石武器槽合并** `Ag.A(a,b)`：武器槽 + 护石武器槽合并成 `dd`（槽位虚拟装备）参与插珠，非转技能点
3. **珠填充是独立搜索** `Pg/Qg/Rg/Sg`：叶子处用 `push` 逐步放珠 + `Ng`（含降级链的槽检查）+ 珠子库存上限，精确求解
4. **候选桶合并** `qe.D()`：含需求技能防具保留个体，其余按 部位×槽位 分桶只留 1 代表

### Python 侧修复清单（fast_search_v3.py）
1. **`_greedy_deco_check_with_future` 武器部分误杀**：单技能珠估算孔位会高估（复合珠可一珠补多技能），改点数上界 `_w_max_pts_per_slot`（武器珠池最大单珠点数）
2. **per-candidate break 误杀**：装备 score 不含武器技能价值，`q_score+remaining_after < def_score*WEIGHT` 会把"仅武器技能"全部候选 break 掉 → 阈值扣除武器插珠覆盖量（`_w_cover_pts`，需 `remaining_wslot_max` 预计算）
3. **防具部分 SLC-FAIL 误杀**：防具赤字直接按单技能珠估算孔位（挑战者5点→需5个Lv3槽），未扣剩余防具自带技能 → 用 `remaining_skill_max[depth]` 削减赤字（`_gap = _d - rsm[_i]`）
4. **武器候选桶合并**：416 件武器槽位全相同仅技能不同，扩展桶合并到 pi==6；插槽技能（`_is_slot_skill`）无区分度不阻止合并
5. **护石放最后**：`part_order` 护石从首位移到倒数第二，先剪防具组合树再试少量护石（原来正解护石靠后时需遍历完前面护石的全部分支）

### 教训
- 参照项目武器槽是独立模型（`Ag`），不是把护石武器槽转成"武器Lv1插槽"技能点
- 中间剪枝凡是"单技能珠估算"都必须警惕复合珠误杀，精确求解交给 fill_slots/珠填充搜索
- 搜索树爆炸时优先看候选是否可桶合并、part_order 是否让正解分支靠后

## 用户需求汇总
1. 武器技能选框有空选项（"留空·自动匹配最优"），留空时搜索后自动选择最优
2. 武器技能库可禁用技能，禁用后不参与自动匹配
3. **固定武器技能=预筛选武器，不是框定技能范围**
4. 搜索必须能出方案，自检通过再提交
5. 武器技能(攻击/看破等)也可由防具提供，不能仅靠武器孔+护石判断可行性
6. 技能选择界面支持直接取消技能（等级选择框含"取消"按钮）
7. 各模式（技能选择/已选/追加）收藏技能同步置顶
8. 追加收藏模式必须显示所有收藏技能
9. 方案区域技能不折叠，始终显示
10. 系列/组合技能与其他技能显示方式一致，不加件数徽章
11. 方案区域有"追加技能"按钮，可基于方案现有技能追加并重新搜索
12. 方案对比支持多选（勾选），对比伤害详细计算过程
13. 查询模式按钮下移：先模式切换再主按钮
14. 追加查询的禁用模式排除已禁用技能
15. 技能组支持编辑名称、删除、上下移动排序
16. 追加模式内的"追加并重新搜索"应调用查询追加（runExtraQuery）而非查询方案（runSearch）
17. 已保存方案详情增加"再次查询"功能
18. 术语规范："套装技能" = 系列技能 + 组合技能（NO_DECO_SK）

## UI设计规范
- 暗色主题：--bg #161922, --card #1e2430, --accent #7c9cff
- 圆角卡片 border-radius: 14px
- 微动画：hover上浮、颜色过渡、tab切换 fadeIn 动画
- CSS变量统一管理颜色
- damage-big 伤害数字使用三色渐变 + drop-shadow 发光
- detail-table 圆角边框 + best-row 绿色高亮
- header 底部发光线 (::after 伪元素)

## 统一算法重构（已完成 2026-08-11）
1. **候选精选重构**：实现统一 Phase 2 Election，每部位每技能选 best + more，不分普通/系列技能
2. **删除系列优先搜索**：移除 `_sf_bt`（约363行）及 `_sf_keys`/`_sf_results` 回退逻辑
3. **统一 DFS**：所有技能走同一套搜索，系列件数作为内部约束检查
4. **统一剪枝**：早期技能点数、系列件数、孔位+珠子可行性统一剪枝
5. **保留优化**：增量维护、fill_slots 预留槽隔离、calc_damage 缓存等

### 武器自动匹配平权修复（2026-08-11）
- 移除 `_generate_weapon_equipment` 中的系列/组合技能预筛选逻辑
- 未固定武器技能时生成全部 816 种武器装备（25系列×2等级 + 15组合 + 750系列+组合 + 1无技能）
- 固定武器技能时仍只生成对应技能的武器（性能优化）
- `dfs_search_auto_weapon` 改用 ThreadPoolExecutor 并行搜索（最多16线程）
- 每武器方案上限降至 3（scout模式），合并后按伤害排序取前 N

### 实测性能（2026-08-11）
- 系列技能（黑蚀龙之力 Lv2）：0.029s（3 plans）
- 组合技能（巨戟龙的默示录）：0.027s（3 plans）
- 系列+组合（火龙之力2 + 霸主之魂3）：0.076s（3 plans）
- 多技能（2普通+1系列+1组合）：0.039s（5 plans）
- 自动匹配全武器（816 weapons）：~8s（20 plans，8线程并行）

### 正确性验证
- 黑蚀龙之力 Lv2：top plan 含 3 件黑蚀龙系列防具 >= 需求 2 件 ✅
- 巨戟龙的默示录：top plan 含 5 件巨戟龙系列防具 >= 需求 3 件 ✅

### 极限突破机制（2026-08-11）
- R5防具：三个插槽各升1级，最高3级
- R6防具：前两个插槽各升1级，最高3级
- 仅限R5/R6级别防具
- 代码位置：`fast_search_v3.py` `limit_break()` 函数
- 数据来源：`armors_cn.json` 需包含完整R5/R6防具数据（含α+/β+等极限突破版本）
- 动态生成：每件R5/R6基础防具自动生成 `+` 升级版，总防具数 1090
- 2026-08-13 修复：基础版防具**不应**应用 `limit_break`（否则动态生成的 `+` 版会在已突破的 slots 上再突破一次，孔位虚高一级，如雪狮子王腰甲β+ 错误显示 `[3,3,0]`，正确为 `[2,2,0]`）。修复后基础版用原始 slots，`+` 版 = 单次极限突破，全库 1090 件孔位合法（≤3孔、≤Lv3）
- **`α+`/`β+` = 极限突破版，动态生成，不是数据缺失**：`limit_break()` 在加载时对每件 R5/R6 基础防具生成 `name+'+'` 升级版（R5 三槽各+1、R6 前两槽各+1，上限3级），`armors_cn.json` 中不存在 `+` 版属正常，无需也不应补齐
- 真正缺失的数据：部分技能（格挡性能、超会心、缓冲、会心击【属性】、龙属性攻击强化、利刃）在防具数据中完全空白（0件自带，仅能从珠子获得），导致特定配装需求无法满足 —— 这才是 155 vs 1 的数据根因

## 网页配装器 30 条 vs 本算法 —— 已修复漏解（2026-08-14 最终，30/30 覆盖）

**需求**：耳塞2/精神抖擞3/格挡性能3/超会心3/缓冲1/快吃3/挑战者5/连击5/会心击【属性】3/龙属强3/利刃3 + Lv1插槽2 + 毅力【果断】(霸主之魂3件) + 黑蚀一体Ⅰ(黑蚀龙之力2件)，护石=盛世护石。

**结果（最终）**：网页版 30 条，本算法 **30 条，覆盖 30/30（缺失 0）** ✅。正确性彻底解决。

### 漏解根因与修复方案（最终采用）
候选支配过滤用**需求技能维度**比较（`_def_dims`），把"需求贡献相同但非需求技能不同"的合法防具误删：
- 黑蚀龙护腿α（铺鳞之技法1/减轻胆怯2）vs β（叠鳞之工艺1/减轻胆怯1）：需求贡献相同（无我之境1/黑蚀龙之力1），β 孔更多 → α 被误判支配剔除
- 祭典护腿α+、暗器蛸腕甲α+/β+、踊火护腿α+ 等同样被需求维度支配剪掉

**修复 = 保持需求维度支配（树小、快）+ 结果展开变体**（`_variant_map` + `_expand_variants`，fast_search_v3.py）：
1. 搜索仍用需求维度支配（候选 ~17/部位，树小）
2. `_variant_map`：每部位按"需求技能贡献"分组（忽略稀有度），含全部 parts 防具
3. `_try_fill_and_record` 记录解后调用 `_expand_variants`：把解的 5 防具部位替换为需求贡献组的其他成员，逐个 fill_slots + 系列件数校验，输出合法变体
4. 结果上限（results）受 max_results 限制；展开组合不设上限（需求贡献组通常 2-10 成员）

**关键点**：
- 展开基于"需求贡献相同"（忽略稀有度），所以跨稀有度的合法变体（如巨戟龙护腿α + 祭典护腿α+，都需求贡献 {挑战者1}）都能补上
- 跳过空需求贡献组（sig=()，无需求技能的防具不会被搜索选中，展开也无意义）
- 展开时逐变体 fill_slots 校验（孔位不同），非法变体丢弃，故不漏解不误杀

### 性能现状（2026-08-15 已充分探索，安全最优）
- 30 解枚举：**5.6s**（原 12.3s，网页版 0.2s，差 28 倍）——已用网页版评分上界剪枝优化
- 30/30 正确性保持，无解B depth0 秒判、`_verify_set2.py` 有解场景全通过

### 性能优化进展（2026-08-15 最终结论）
1. ✅ **变体展开**（`_variant_map` + `_expand_variants`）：30/30 覆盖（原 15 漏解）
2. ✅ **网页版评分上界剪枝** `q_score*L + cur_slot_total*SKILL_WEIGHT < _gear_def_pts*SKILL_WEIGHT`（~2755，`_remaining_part_count=6-depth`）：13→5.6s，30/30 未误剪
3. ✅ **达标即返回**：deficit=0 且系列满足时记录后 return——但因需求技能多、赤字前期不为 0，收益有限

### 评分剪枝安全边界（已实测，勿再走错路）
| 剪枝变体 | 耗时 | 正确性 |
|---------|------|--------|
| `q_score*L + cur_slot_total*SW`（含孔位，当前） | **5.6s** | 30/30 ✅ 安全 |
| 纯技能分 `Q.i*L`（网页版语义，不含孔位） | 2.2s | **25/30 ❌ 误剪**（跨部位 best 技能分可能 > 当前候选，低估致漏解） |
| 剩余各部位 best 之和（`remaining_best_skill_sum+slot`） | 14s | 30/30 ✅ 但太松 |

**核心矛盾（验证清楚）**：要 30 解全出（不误剪），评分剪枝必须用"安全上界"（各部位 best 之和或含孔位），而安全上界是松的 → 慢；网页版用紧的纯技能分 `Q.i*L` 会跨部位低估、误剪，它"找高分解"可接受，我们 30 解全要不能接受。

### 剩余性能瓶颈
节点分布 `[1, 10, 88, 1317, 11716, 4367, 16077]`，profile：`_dfs` 49653 次 4.9s + `dict.get` 703万 1.3s + `_g6_core` 32万 1.18s + `fill_slots` 18706 次 2.7s。
- depth4 11716、depth6(武器) 16077、depth5 4367 为主
- 网页版 0.2s 部分是 **C 级编译 JS + 不同搜索语义**（达标返回+聚合），非纯算法差距；Python 解释器开销（dict.get 703万、fill_slots 1.9万）也占大头

**要对齐 0.2s** 的两条路（均高风险）：
①重构为网页版"聚合搜索+达标返回+展开"完整范式（聚合曾漏解，需解决展开覆盖）
②Python 底层向量化（dict→list/numpy，大改，收益不确定）

当前 5.6s/30/30 是"评分剪枝+穷举代表解"架构下的安全最优值。

### 验证脚本
`_diag_verify30.py`（30/30 覆盖对比）、`_diag_force_ab.py`（强制各 β/α 防具均出解）、`_diag_ab_revisit.py`（α/β 配装技能差异）、`_diag_variant_size.py`（需求贡献组规模）、`_diag_maxresults_sweep.py`（max_results 对覆盖/耗时影响）、`_diag_prof_full.py`（cProfile）、`_diag_depth_nodes.py`（各 depth 节点分布）。

## 珠子/槽位体系：武器珠与防具珠是两套独立池（2026-08-13 源码实证，勿再走错路）

**来源**: `docs/source/sim-compiled-ja.js.es6.js`（荒野 wiki-db 配装器编译产物，本项目的参照实现）

### 核心结论（双池独立，不是全局一池！）
- `md.ca` = **武器珠池**（攻击珠等，type='weapon'）
- `md.Aa` = **防具珠池**（防御珠等，type='armor'）
- 两池完全独立，**绝不混用**：武器技能只能插武器槽、用武器珠；防具技能只能插防具槽、用防具珠
- 武器槽和防具槽是两个独立容量池，不合并

### 源码证据
1. `var md={Aa:[],ca:[],lb:{}}` —— 双池容器定义
2. `Gh` 构造器：`od.Ea.has(m)` 将武器技能从普通技能需求中分离到 `this.S`
3. `dh.C()`：若珠子技能含武器技能（`od.Ea.has(d)`）则**不扣防具槽** —— 武器珠占用武器槽的铁证
4. 武器槽是独立装备类 `Ag`（"武器スロ"），`Y.i()` 枚举所有武器槽组合
5. UI 分开管理："武器装飾品"（来自 `md.ca`）与防具珠（`Vd→Td→Sd(md.Aa)`）
6. 保存/加载用两个独立字段：`decos`/`X`=防具珠，`bodyDecos`/`A`=武器珠

### 对应 Python 实现（正确方向）
- `_get_deco_pool('weapon')` = 武器珠池（`_build_deco_pool_full('weapon')`）
- `_get_deco_pool('armor')` = 防具珠池（`_build_deco_pool_full('armor')`）
- `fill_slots`/`can_fill_gap`/`_check_deco_feasible` 必须**保持分池**：武器技能查 `deco_idx[(sk,'weapon')]`，防具技能查 `deco_idx[(sk,'armor')]`
- **严禁**将武器槽+防具槽合并为统一容量池、严禁让武器技能从全局珠池取珠（这是本次会话发现的错误方向，已回滚）

### 术语对照
- 珠子数据：`decos_cn.json` 每条含 `type`（weapon/armor）、`slot`（1/2/3）、`skill1`/`skill2` 及等级

## 预留空插槽 / 插槽技能逻辑（2026-08-13 参照项目复核）

**需求例子**：`Lv1插槽 Lv3`（要求防具上至少有 3 个 Lv1 及以上空槽）、`武器Lv2插槽`、`防具Lv1插槽` 等。这类技能**不是珠子技能，由装备孔位贡献满足**。

### 参照项目（wiki-db）实现机制
1. **空槽护符（スロお守り）**：`oe()` 生成 `skills={}`（无技能）、`slots=1/2/3`、`part=5` 的虚拟护符装备，`ra()` 转成 `Zc` 候选进入搜索池。空槽装备无技能但带孔，专用于凑插槽技能需求。
2. **槽位搜索虚拟装备（Dd 类）**：按槽位等级（`f`=1/2/3）分类的虚拟装备，`c`=槽位大小计数 `{1,2,3,4,99}`。搜索中作为候选，靠槽位贡献满足插槽技能。
3. **插槽技能由孔位贡献满足**，不是珠子：每个孔把 `s` 换算成 `Lv1..Lv(s)插槽` 各 +1。

### Python 侧对应（fast_search_v3.py 已实现，勿改坏）
- `SLOT_SKILLS` / `_is_slot_skill(sk)`：`sk.endswith('插槽')` 即插槽技能
- `_slots_to_skills(slots, prefix)`：孔位列表 → `Lv1/2/3插槽`（prefix=''）、`防具Lv1/2/3插槽`、`武器Lv1/2/3插槽` 贡献
- **预留槽位隔离**：`fill_slots` 中插槽技能需求的孔先从可插池物理移除（`reserved_a/reserved_w`），防止贪心插珠把低阶珠插进预留槽导致最终校验失败
- **插槽技能无珠子、不判死**：`_check_deco_feasible` 及各处对 `_is_slot_skill` 的技能 skip 珠子可行性检查
- **预留插槽上限二分**：`query_extra` 里 `_side_slot_bound` + 二分，按 Lv1>=Lv2>=Lv3 单调性逐级收窄，上界封顶 6

## 无解判定专项（2026-08-14 已完成）

**目标**：无解场景不能等 30s 超时，需要"判断无解的逻辑"，参照网页配装器（wiki-db）做法。

### 网页配装器判无解逻辑 —— 已探明（重要结论）

**网页版没有显式"无解判定"，它的"判无解" = 搜索空间压缩到能穷举完 + 穷举完返回空**。机制四件套：

1. **候选压缩（根本）**：
   - `Pd` 套装聚合：同部位、同孔位、技能完全相同 → 合并成一个候选
   - `Qb` 候选层支配过滤：搜索前用 `Vd` 剔除被更优候选支配的装备（仅快速模式）
2. **case 1 提前构建**：`A <= M`（目标分 ≤ 已放装备孔位数之和 `Ud`）时，用 `Rb`（珠子补位检查，含槽位降级链 `0>e&&(p+=e); 0>p&&(k+=p)`）尝试直接填满返回
3. **剪枝1**：`D.P.f * Pa + M < A` 评分阈值 break（f=普通防具0/槽位虚拟装备=槽位等级）
4. **剪枝2**：`jf(u, hb)` 搜索中实时支配检查 + 结果上限 200 + 每步超时

**Python 版与网页版的本质差异**：网页版每个技能点=1颗珠、槽位降级链精确判定；Python 版 `_greedy_deco_check_with_future` 是近似估算。网页版靠"空间小→穷举完"，Python 版空间大无法穷举，只能靠剪枝。

### 无解场景分类（诊断脚本 `_diag_nosol*.py`）

| 场景 | 特征 | 现状 |
|------|------|------|
| 无解A/C/D | 预检查可判（总和/系列件数/孔位） | ✅ 0.02s 秒杀 |
| **无解B** | 技能+孔位联合组合约束 | ✅ depth0 判死，秒返回 |

**无解B 画像（修复后）**：需求 43 点技能，装备可达上界仅 20 点，23 点缺口全要靠珠子；真实武器孔容量仅 12 点（3 个 Lv3 槽），武器侧赤字 22 点必死；装备侧 rsm 预支总量被 `remaining_skill_total` 上界按比例缩减后，depth0 即判"珠子补不满" → 判死。

### 本次会话改动（fast_search_v3.py）

| 改动 | 结果 |
|------|------|
| ✅ `remaining_slot_by_lv` 上界修复：每部位取"槽位点数最大"的**单件**候选累加完整分布（原各等级独立取最大来自不同装备、虚高；武器部位不再累加，WSLOTS 已由 `_w_slot_cnt` 计入） | 保留 |
| ✅ 等价候选桶合并：无贡献防具桶合并 → 全签名 `(nz, slots_sorted, wslots_sorted)` 等价合并（`_has_req_series` 保留个体） | 武器 42→22，保留 |
| ✅ `remaining_skill_total`（每部位取单件 sv 总和最大累加，真实可达）：`_greedy_deco_check_with_future` 中 depth0 预支总量超上界则按比例缩减 rsm | **无解B depth0 判死**，有解不误杀 |
| ✅ `_greedy_deco_check_with_future` 从"仅 total_demand>0"改为 depth<6 无条件调用 | 保留 |
| ⚠️ Qb 候选级支配过滤实现（技能/孔位/分数全≥剔除被支配者） | **无效**：候选已高度帕累托化，零剔除 |
| ❌ break 检查武器孔重复计入修复（`cur_slot_total` 含9个武器孔×SKILL_WEIGHT 虚增900分） | **误杀正解已回滚**，保留原 `cur_slot_total*SKILL_WEIGHT`（每孔1点） |
| ⚠️ depth<7 检查扩展 | **误杀正解**：depth=6 时 armor_min 双重扣减把已选防具孔扣瘦（9孔→3孔）。保留为 `depth==6 and not armor_min and not weapon_min` 才调用 |
| ✅ depth<=6 时 `rsl` 提供武器孔位上界（`remaining_slot_by_lv[6]` 只含武器） | 保留 |

**当前状态（2026-08-14 完成）**：
- 无解B：depth0 判死，秒返回（`_diag_nosol16.py` 输出 `depth分布: [1,0,...]`）
- 有解（`_verify_set2.py` 同 fixed/combo）：结果数=1、1.8s、14 项需求全部满足 ✅
- 有解 exhaustive（`_bench_full2.py`，max_results=3 需证明仅 1 解）：52.7s（修复前 90s 超时，已改善）
- 修复手段对照网页版：候选压缩（等价桶合并 = `Pd`/`Qb` 语义）+ 中间珠子补位检查（`Rb` 降级链已有）+ 预支总量约束（新增）

### 无解B 最终修复方案（2026-08-14 已实现并验证）

**根因**：`rsm`（remaining_skill_max）逐技能独立取各部位候选最大并累加 → 组合上不可达（选"挑战者最多"的装备就选不了"耳塞最多"的装备），depth 浅层把预支全部用掉，珠子降级链永不触发、无解判不死。

**修复链**（对照网页版四件套）：
1. 候选压缩：等价候选桶合并（`Pd`/`Qb` 语义）→ 武器 42→22
2. `remaining_slot_by_lv` 上界修复：每部位取单件完整分布，武器孔不再双计（真实 w_cap=12）
3. **`remaining_skill_total` 预支总量约束**（`_greedy_deco_check_with_future` 内，仅 depth0）：`_pre_total > _cap_total` 时按比例缩减 rsm → 无解B depth0 判死
4. 降级链（`0>e&&(p+=e)`）珠子补位检查已有，配合缩减后生效

**教训**：
- 预支缩减若**全深度**生效会误杀有解（深层剩余部位少、逐技能最大接近真实、缩减过狠），必须**仅 depth0**
- 直接**零预支**也会误杀（有解 w_def=13 略超真实容量 12，靠缩减+深层 rsm 放行）
- 武器侧真实容量 12 点（w_cap=12）确认为判死依据：无解B 武器技能赤字 22 点必死

### 临时诊断脚本（用户要求保留，勿删）

`_diag_nosol*.py`（无解场景）、`_diag_brk_sol.py`（有解 break 诊断）、`_diag_d6trace.py`（depth6 追踪）、`_diag_prof.py`（cProfile）、`_diag_equiv.py`、`_bench_full2.py`/`_bench_layers2.py`（基准）、`_verify_set2.py`（有解回归）、`_diag_nosol16.py`（无解B depth 分布）。
- 已清理：DIAG0 3 方案对比调试块（2026-08-14 修复完成后移除）
- 残留：fast_search_v3.py 中 `_DIAG`/`_TRACE`/`_DIAG_DEPTH` 守卫的调试打印（默认 False，供诊断脚本使用），提交前可清理
